server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

# FIX: Upload path - apne system ke hisaab se change karo
# Windows example: D:/MyLocker/uploads/
# Linux/Mac example: /home/yourname/mylocker-uploads/
file:
upload-dir: D:/Interview/My_locker/springbootBackend-FINAL-v5/uploads/documents

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: vivekumar0709@gmail.com
    password: jdzahvnctmvjtdsh
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:mysql://localhost:3306/registration
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
